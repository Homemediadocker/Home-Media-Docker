networks:
  homelab:
    driver: macvlan
    driver_opts:
      parent: eth0
    ipam:
      config:
        - subnet: 192.168.86.0/24 #update if needed
          gateway: 192.168.86.1 #update if needed
  default:
    driver: bridge
    name: home-media-docker_default
  zwave:
